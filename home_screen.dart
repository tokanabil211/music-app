import 'package:flutter/material.dart';

class HomaeScreen extends StatelessWidget {
  const HomaeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
